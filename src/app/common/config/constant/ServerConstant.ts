export enum Protocol {
    TCP = 'tcp',
    UDP = 'udp',
    HTTP = 'http',
    HTTPS = 'https',
    WS = 'ws',
    WSS = 'wss',
    FTP = 'ftp',
}

export enum AddressType {
    IPv4 = 'IPv4',
    IPv6 = 'IPv6',
    URL = 'URL',
}

export enum ServerType {
    main = 'main',
    file = 'file',
    video = 'video',
    voice = 'voice',
}

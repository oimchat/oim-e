export enum Protocol {
    TCP = 'TCP',
    UDP = 'UDP',
    HTTP = 'HTTP',
    HTTPS = 'HTTPS',
    WebSocket = 'WebSocket',
    FTP = 'FTP',
}

export enum AddressType {
    IPv4 = 'IPv4',
    IPv6 = 'IPv6',
    URL = 'URL',
}

export enum ServerType {
    main= 'main',
    file= 'file',
    video= 'video',
    voice= 'voice',
}
